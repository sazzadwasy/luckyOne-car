import React from 'react';

const Ans = () => {
    return (
        <div>
            <p>Qus: How react works?</p>
            <p>Ans : At first react is a javaScript library.It creates user interfaces in a predictable and efficient way using declarative code.</p>
        </div>
    );
};

export default Ans;